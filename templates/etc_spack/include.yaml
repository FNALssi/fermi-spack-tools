include:

  # per os scope
  - path: ${platform}/${os}
    optional: true

  # per platform scope
  - path: ${platform}
    optional: true

  # base scope
  - path: base

  # site scope
  - path: site

